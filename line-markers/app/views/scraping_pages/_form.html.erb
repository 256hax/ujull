<%= form_with(model: scraping_page, local: true) do |form| %>
  <% if scraping_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scraping_page.errors.count, "error") %> prohibited this scraping_page from being saved:</h2>

      <ul>
      <% scraping_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :scraping_frequency %>
    <%= form.number_field :scraping_frequency %>
  </div>

  <div class="field">
    <%= form.label :target_element %>
    <%= form.text_field :target_element %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
